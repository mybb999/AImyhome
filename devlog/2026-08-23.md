# 开发日志 — 2026-08-23 (周日)（收尾工作延续至 08-24 凌晨）

## 已完成事项

- [x] 修复「博客路由快速切换时左侧个人简介被渲染到 aside 外部/页面底部」的布局 Bug（详见「遇到的问题」1）
- [x] 清理 `pages/blog/[slug].vue` 模板中的调试残留文本 `asdasd`
- [x] 布局职责重构（设计文档 `docs/superpowers/specs/2026-08-23-layout-refactor-design.md`，SDD 6 任务 + 逐任务审查 + 终审 + 修复波）：
  - `layouts/default.vue` 瘦身为纯外壳（仅 AppHeader + slot，无业务组件、无路由判断）
  - 新增共享外壳组件 `components/shared/AppHeader.vue`（顶栏）与 `components/shared/AppShell.vue`（两栏骨架：#sidebar 插槽 + 默认插槽 + 页脚）
  - 四个页面各自组装侧栏：首页/AI=AvatarCard、博客列表=BlogSidebar、博客详情=BlogToc（toc 直接 prop 传入）
  - 删除 TOC 的 provide/inject（setBlogToc）机制，全仓零残留
- [x] 修复波（终审发现项）：修正 `pages/blog/[slug].vue` 的 TocItem 类型导入路径（`~/components/blog/BlogToc.vue`）；修订 CLAUDE.md 组件规范（允许纯模板组件省略 script 块）；设计/计划文档入库
- [x] 回归验证：Playwright + Edge 无头探针（四路由侧栏渲染、移动端单列、快速切换竞态探针 0 异常）在干净工作区全部通过；`npm run build` 通过
- [x] 全部 13 个提交推送到 origin/master（`83a9003..1c76e97`）

## 待办事项

- [ ] （可选加固）评估 `nuxt.config.ts` 中 `pageTransition.mode: 'out-in'` 的快速切换打断风险
- [ ] （可选）把回归探针脚本（check.js / storm.js，目前在 %TEMP%/pw-driver）固化进 `scripts/e2e/`
- [ ] （可选）AppHeader 三链接高亮逻辑去重；页脚年份动态化

## 遇到的问题

1. **Bug**：首页/聊天工具 → 博客快速切换（BlogSidebar 文章列表未渲染完时切回）时，AvatarCard（个人简介）被渲染到 `aside` 外部、页面底部，aside 变空。
   **根因**：`components/blog/BlogSidebar.vue` 使用 `await useFetch` 使组件成为异步组件（挂起在 Suspense 中）。挂起未 resolve 时组件被卸载（路由切走），数据返回后 Vue 将 aside 的 v-if 分支内容插入到错误父节点（布局根 `div.min-h-screen`），导致 AvatarCard 被挤出 aside。「out-in」页面过渡放大了被打断的概率。
   **修复**：移除 BlogSidebar 中的 `await`，改为非阻塞 `useFetch`（与 BlogList 一致，`default: () => []` 已处理 pending 状态），组件不再挂起。
   **验证**：Playwright + Edge 无头复现（CPU 节流 4x + /api/blog 延迟 1s + 60~250ms 弹回切换），修复前稳定复现、修复后全时序干净。
   **教训**：侧栏/常驻区域不要放 `await useFetch` 的异步组件；重构后布局内已无路由 v-if 切换，该问题类别整体消除。

2. **重构过程发现**：
   - 终审指出：Bug 修复本体最初只存在工作区未提交，导致验证证据与 committed 状态脱钩——修复波中补提交并在干净工作区重跑探针对齐。
   - 计划断言 `grep -c "返回博客"` 期望 1 实为 2（BlogToc 侧栏按钮 + BlogContent 返回链接各一处，子串匹配），属断言口径问题非缺陷。
   - dev 服务器只监听 IPv6 `[::1]:3000`（127.0.0.1 拒绝连接）；`npm run build` 与 dev 服务器互斥，构建前需先释放 3000 端口。

## 下一步

- 用户回归测试后确认线上效果
- 评估停放的三个可选优化项（见待办）
