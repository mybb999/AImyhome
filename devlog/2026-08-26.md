# 开发日志 — 2026-08-26 (周三)

## 已完成事项

- [x] AI 聊天工具多模型支持：服务端 provider 配置表 + 白名单校验（server/utils/llm.ts），GET /api/agent/models 模型列表，POST /api/agent 按 model 路由（豆包 doubao-seed-2-0-lite-260215 / 智谱 glm-4.7-flash），ChatPanel 下拉选择器（localStorage 记忆、流式中禁用）
  - `.env.example` 新建：LLM_*（智谱）必填示例 + DOUBAO_API_KEY（可选，配置后前端下拉自动出现豆包选项）
  - `docs/ARCHITECTURE.md`：架构图中 `server/api/agent.ts` 引用更新为 `server/api/agent.post.ts`（一行文档修正）
- [x] 豆包路由假 key 实测（临时在 `.env` 追加 `DOUBAO_API_KEY=fake-key-for-routing-test`，验证后已删除、`.env` 恢复原状）：
  - `GET /api/agent/models` 返回 doubao 首位 + glm 第二（key 过滤生效）
  - `POST /api/agent` 带 `model: "doubao"` → 502，错误体 `The API key format is incorrect`（火山方舟对非法格式 key 的认证层报错）——证明请求确实路由到 `ark.cn-beijing.volces.com/api/v3`（baseURL/model 正确，仅认证失败）
  - 删除假 key 后 `GET /api/agent/models` 恢复为仅 glm；无 model 默认请求实测 SSE 流式正常（glm-4.7-flash 返回真实内容）；未知 model 返回 400 `unknown model: xxx`
  - 浏览器验证（playwright-core + Edge headless）：选择器默认显示「豆包 Seed-2.0-Lite」→ 切换「智谱 GLM-4.7-Flash」→ 刷新后仍为智谱（localStorage 记忆生效）

## 待办事项

- [ ] 注册火山方舟并配置 DOUBAO_API_KEY（本地 .env + Vercel 环境变量）后实测豆包

## 遇到的问题

1. **豆包假 key 实测结果**：认证层报错 `The API key format is incorrect`（502），说明路由、baseURL、model 全部正确，只差真实 key。注意：`doubao-seed-2-0-lite-260215` 若真实 key 下仍报 model 相关 400/404（火山方舟部分模型要求 `ep-xxx` 接入点 ID 而非模型名），需将 `server/utils/llm.ts` 的 `DOUBAO_MODEL` 换成接入点 ID —— 待真实 key 实测确认。
2. **sel2.js 的 count() 时序假象**：简报脚本在 1.5s 采样 `button.chip` count 恒为 0，但 `textContent()`/`click()`（自动等待）全部正常。原因：ChatPanel 的 models 列表是 onMounted 异步 fetch `/api/agent/models` 后才渲染选择器，dev 下首次加载耗时 >1.5s；playwright `count()` 不做自动等待，`textContent()` 会等。等 4s 后实测 `button.chip` count = 1（DOM 确认）。非功能缺陷，是验证脚本采样时机问题。
3. 本地 `.env` 无 DOUBAO_API_KEY（grep 确认），故按简报走假 key 流程；验证后 `.env` 恢复原状（148 字节、3 个变量、无残留）。

## 下一步

- 用户注册火山方舟拿到真实 DOUBAO_API_KEY 后：填入本地 .env + Vercel 环境变量 → 实测豆包 SSE 流式（若报 ep-xxx 接入点错误则改 DOUBAO_MODEL）→ 确认前端下拉双模型可用
